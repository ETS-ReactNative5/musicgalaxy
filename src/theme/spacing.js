import {scale} from '../utils/media';

export const spacing = {
  xsmall: scale(4),
  small: scale(8),
  medium: scale(12),
  default: scale(16),
  large: scale(24),
  xlarge: scale(32),
  huge: scale(48),
};

export const radius = {
  default: scale(4),
  double: scale(8),
  half: scale(30),
  full: scale(50),
};
